import { ReactNode } from 'react';

export interface ProviderPrimaryPatient {
  name: ReactNode;
  email: ReactNode;
  phone: ReactNode;
  unit: ReactNode;
  serviceArea: ReactNode;
  gender: ReactNode;
  country: ReactNode;
  status: ReactNode;
  createdAt: ReactNode;
  age: ReactNode;
  action: ReactNode;
}
